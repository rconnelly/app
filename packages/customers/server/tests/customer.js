'use strict';

/**
 * Module dependencies.
 */

//var should = require('should');
  //var mongoose = require('mongoose');
  //Customer = mongoose.model('Customer');
  //_ = require('lodash');


/**
 * Test Suites
 */
describe('<Unit Test>', function() {
  describe('Model Customer:', function() {

    before(function(done) {



      done();
    });

    describe('Validation', function() {
      it('should have correct required fields', function(done) {
        //var result = _.difference( Customer.requiredPaths(),[]);
        //should(result).have.property('length').with.lengthOf(3);
        done();
      });
    });

    after(function(done) {

      done();
    });
  });
});
