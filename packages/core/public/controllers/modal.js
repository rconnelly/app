'use strict';

angular.module('mean.core').controller('ModalController', ['$scope', 'Global', 'Core','ModalService',
  function($scope, Global, Core, ModalService) {
    $scope.global = Global;
    //$scope.
  }
]);
